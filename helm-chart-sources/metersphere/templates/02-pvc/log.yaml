kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: metersphere-logs-pvc
  namespace: {{.Values.metersphere.namespace}}
spec:
  accessModes:
    - {{.Values.storage.accessModes}}
  resources:
    requests:
      storage: {{.Values.storage.logs.capacity}}
  storageClassName: {{.Values.storage.storageClassName}}